# Server boundaries

Server-only application logic, background jobs, workers, and browser
automation integrations should be added here as those capabilities are
implemented. Keep external services behind small, explicit interfaces.